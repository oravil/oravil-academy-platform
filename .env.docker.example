# PostgreSQL
POSTGRES_DB=oravil_academy
POSTGRES_USER=oravil
POSTGRES_PASSWORD=oravil_secret
POSTGRES_PORT=5432

# pgAdmin (optional – only used with: docker compose --profile tools up -d)
PGADMIN_DEFAULT_EMAIL=admin@oravil.dev
PGADMIN_DEFAULT_PASSWORD=admin_secret
PGADMIN_PORT=5050
